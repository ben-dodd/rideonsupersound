import MidScreenContainer from 'components/container/mid-screen'

const OrdersScreen = () => {
  const isLoading = false
  return (
    <MidScreenContainer title="ORDERS" isLoading={isLoading} titleClass="bg-col5" full={true}>
      <div />
    </MidScreenContainer>
  )
}

export default OrdersScreen
